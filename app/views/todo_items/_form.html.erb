<%= form_for([@todo_list, @todo_list.todo_items.build]) do |f| %>
  <%= f.text_field :content, placeholder: "New Todo", class: "form-control form-group" %>
  <%= f.submit class: "form-group btn btn-success new-btn btn-lg"%>
<% end %>
